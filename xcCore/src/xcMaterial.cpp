#include "xcMaterial.h"

namespace xcEngineSDK {

    bool 
    Material::loadFromFile(const String& filePath) {
        return false;
    }

    bool 
    Material::loadFromMemory(const char* pData, size_t sizeOfData) {
      return false;
    }

    void 
    Material::unload() {
    }

    void 
    Material::saveToFile(const String& filePath) {
    }
}